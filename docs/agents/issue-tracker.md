# Issue tracker: not currently used

**English** | [Deutsch — canonical](issue-tracker.de.md)

The user has not chosen an issue tracker for the current early product-development phase. Local Markdown tickets and triage labels are also not configured.

When a skill requests creating or publishing a ticket, skip that step. Record substantive results in the existing locations according to the [documentation rules](domain.md). There are no ticket numbers, status transitions, or assignments to manage.

When a skill expects a ticket as input, use the user's topic and the relevant existing documents. Missing tickets must not block substantive work. Introduce a task-tracking process later only at the user's request.
